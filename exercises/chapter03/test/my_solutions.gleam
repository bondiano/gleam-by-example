//// Здесь вы можете писать свои решения упражнений.

/// Применяет функцию f к значению x дважды.
pub fn apply_twice(f: fn(a) -> a, x: a) -> a {
  todo
}

/// Добавляет восклицательный знак к строке.
pub fn add_exclamation(s: String) -> String {
  todo
}

/// Переводит строку в верхний регистр и добавляет "!".
/// Используйте pipe-оператор.
pub fn shout(s: String) -> String {
  todo
}

/// Безопасное целочисленное деление.
/// Возвращает Error("деление на ноль") при делении на 0.
pub fn safe_divide(a: Int, b: Int) -> Result(Int, String) {
  todo
}

/// FizzBuzz: если число делится на 15 → "FizzBuzz",
/// на 3 → "Fizz", на 5 → "Buzz", иначе → строковое число.
pub fn fizzbuzz(n: Int) -> String {
  todo
}
